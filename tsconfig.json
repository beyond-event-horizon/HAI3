{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": [
    "HAI3 TypeScript Configuration (Root)",
    "",
    "This file extends the monorepo preset which extends standalone.",
    "Architecture: presets/standalone/configs/tsconfig.json -> presets/monorepo/configs/tsconfig.json -> tsconfig.json (this file)",
    "",
    "NOTE: Unlike JS configs, tsconfig paths are relative to file location.",
    "So 'include' and 'references' must be duplicated at each level.",
    "",
    "DO NOT add compilerOptions here - add them to:",
    "- presets/standalone/configs/tsconfig.json for options that apply to standalone HAI3 projects",
    "- presets/monorepo/configs/tsconfig.json for monorepo-specific options"
  ],
  "extends": "./presets/monorepo/configs/tsconfig.json",
  "compilerOptions": {
    "baseUrl": "."
  },
  "include": ["src", "packages/uikit/src/styles/tailwindColors.ts"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
