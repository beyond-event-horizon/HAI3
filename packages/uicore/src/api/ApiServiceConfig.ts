/**
 * API Service Configuration
 * Base configuration shared across all protocols
 */

/**
 * Base API Service Configuration
 * Contains only the configuration shared across all protocols
 */
export interface ApiServiceConfig {
  baseURL: string;
}
