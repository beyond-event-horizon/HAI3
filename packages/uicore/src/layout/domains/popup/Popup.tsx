import React from 'react';

/**
 * Popup Component (Placeholder)
 * Reserved for future modal/dialog functionality
 * Currently not rendered or used anywhere
 */
export const Popup: React.FC = () => {
  // TODO: Implement popup functionality when needed
  // Will require:
  // - Popup actions (emit events, not direct dispatch)
  // - Popup effects (listen to events, update slice)
  // - Render in Layout component
  return null;
};

Popup.displayName = 'Popup';
